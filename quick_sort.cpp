#include <stdio.h>
#include <stdlib.h>
void quick_sort(int *a, int length);
int qt(int *a, int low, int high);
int main()
{
    int a[] = {1, 12, 31, 21, 41, 14, 31, 22, 45, 32};
    int length = sizeof(a)/sizeof(int);
}
void quick_sort(int *a)
{
    int low, high;
}
int qt(int *a, int low, int high);
{
    while(low < high) {
        while(low<high && ;wq)
    }
}
